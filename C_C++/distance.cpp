#include <iostream> // 입출력을 위해 임포트
#include "distance.h" // 두 점 사이 거리를 구하기 위해 임포트
/*
int main() {
	float x1, x2, y1, y2; // 입력받을 점 좌표
	float result; // 결과를 저장할 변수를 선언한다.

	std::cout << "좌표를 입력하시오(eg:4 0 0 4): ";
	std::cin >> x1 >> x2 >> y1 >> y2; // 사용자에게 입력 받는다.
	
	result = get_distance(x1, x2, y1, y2); // distance.h의 함수를 호출한다.
	
	std::cout <<"두 점 사이의 거리는 " << result << "입니다" << std::endl; // 결과를 출력한다.
		
	return 1;
}
*/